<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="cat.udl.tidic.a_favour">

    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <!-- Internet Permission -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />


    <permission
        android:name="cat.udl.tidic.a_favour.permission.MAPS_RECEIVE"
        android:protectionLevel="signature" />

    <application

        android:name=".App"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme.NoActionBar">

        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />
        <activity android:name=".Views.EditProfileView" />
        <activity android:name=".Views.ConfigurationView" />
        <activity android:name=".view.MessagesView" />
        <activity android:name=".Views.HelpView" />
        <activity android:name=".Views.RegisterView" />
        <activity android:name=".view.ChatView" />
        <activity android:name=".Views.ProfileView" />
        <activity android:name=".Views.UploadFavour" />
        <activity android:name=".Views.AnunciView" />
        <activity android:name=".Views.MainPage" />
        <activity android:name=".Views.LoginView"/>
        <activity android:name=".Views.CustomActivty"/>
        <activity android:name=".view.FavoursActivity"/>
        <activity android:name=".view.FavoursListActivity"/>
        <activity android:name=".view.FavoursMapActivity"/>
        <activity android:name=".view.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>